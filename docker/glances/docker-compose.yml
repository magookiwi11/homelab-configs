services:
  glances:
    image: nicolargo/glances:latest
    container_name: glances
    restart: always
    pid: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - GLANCES_OPT=-w    # web UI mode
    ports:
      - 61208:61208
